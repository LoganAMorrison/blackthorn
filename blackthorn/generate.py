import argparse

GENERATE_CONSTRAINTS = argparse.ArgumentParser(prog="constraints")
GENERATE_SPECTRA = argparse.ArgumentParser(prog="spectra")

generate = argparse.ArgumentParser(prog="generate")

if __name__ == "__main__":
    pass
